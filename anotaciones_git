Comandos GIT:

git init : crear un repositorio asociado a una carpeta.

git status -s : listar el contenido del repositorio de git(archivos y carpetas).

        ??(en rojo): el archivo o carpeta no tiene seguimiento por git
        A(en verde): el archivo o carpeta pasa al area de ensayo y que esta seguido por git

git add . : pasar el contenido de la carpeta de trabajo al area de ensayo.

git commit -m "texto estado proyecto"

git log --oneline para ver los commits

git reset --hard numero del commit

